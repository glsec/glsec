include:
  - template: Security/SAST.gitlab-ci.yml
  - template: Security/Secret-Detection.gitlab-ci.yml

stages:
  - test
  - security

build:
  stage: test
  image: node:20.11.0
  script:
    - npm ci
    - npm test

sast:
  allow_failure: true

secret_detection:
  allow_failure: true
