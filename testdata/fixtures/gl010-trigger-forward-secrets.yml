stages:
  - build
  - trigger

build:
  stage: build
  image: node:20.11.0
  script:
    - npm ci
    - npm run build

trigger-downstream:
  stage: trigger
  trigger:
    project: org/downstream-project
    forward:
      pipeline_variables: true
