stages:
  - build
  - deploy

build:
  stage: build
  image: golang:1.24-alpine
  script:
    - go build ./...

deploy-prod:
  stage: deploy
  environment: production
  script:
    - ./deploy.sh
  # no resource_group: — concurrent runs possible
